# Real secrets (dev defaults live in docker-compose.yml / .env.example)
.env

# Runtime user databases created by the server
data/

# Local tooling / OS cruft
.claude/
.DS_Store
node_modules/

# The sync engine is an npm dependency, not part of this repo
sync_engine_ts/
